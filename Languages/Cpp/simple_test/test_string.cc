#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {

    string s = "Hello";

    cout << "s.size() = " << s.size() << ", strlen(s) = " << strlen(s.c_str()) << endl; // 5, 5

    cout << "s.length() = " << s.length() << endl; // 5
    

    return 0;
}