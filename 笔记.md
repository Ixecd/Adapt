## 2025-11-9

1. curl -s 中的 -s 代表静默输出，不显示错误信息、警告信息和进度信息
2. curl -v 中 -v 代表verbose 详细输出
   1. 请求信息 > 开头
   2. 响应信息 < 开头
   3. 连接信息 * 开头
3. curl -f 中 -f 代表请求失败时静默失败（不输出HTML错误页面）
4. jq json处理工具，-r 不含引号输出原始字符串， .xxx 从解析出来的json文件中提取xxx字段
5. shell脚本中用于组织和管理函数的常见约定：命名空间函数定语语法**iam::test::login()** 
   1. iam：主命名空间（项目名称）
   2. test：子命名空间（模块名称）
   3. login：具体的函数名
6. URL中查询参数
   1. ？：查询参数的开始富豪
   2. offset=0:分页的起始位置，类似于下标
   3. &：参数分隔符
   4. limit=10：每页返回的记录数量
7. ANSI颜色转义码
   1. \033[32m...\033[0m
   2. \033：ESC字符（ASCII 27）
   3. [32m：设置文本颜色为绿色
   4. \033[0m：重置所有格式
8. shell脚本参数检查和处理机制
   1. $*：所有位置参数的集合
      1. 例如：./test.sh arg1 arg2 arg3 -> "arg1 arg2 arg3"
      2. =~：正则表达式匹配操作符
      3. iam::test:: 要匹配的模式
9. 终端中通过 grep 使用正则表达式

